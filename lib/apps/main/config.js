'use strict';

const config = {
  name: 'cta-app-instance-dataservice',
  tools: [{
    name: 'logger',
    module: 'cta-logger',
    properties: {},
    scope: 'all',
  }],
  bricks: [],
};

module.exports = config;
